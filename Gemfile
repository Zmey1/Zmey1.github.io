# Gemfile
source "https://rubygems.org"

# Use GitHub Pages’ own gem, which pins Jekyll & allowed plugins
gem "github-pages", group: :jekyll_plugins

# Include the cache‑include tag
gem "jekyll-include-cache"
